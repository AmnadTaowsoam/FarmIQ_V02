APP_PORT=3000
DATABASE_URL=postgresql://farmiq:farmiq_dev@postgres:5432/farmiq
CLOUD_INGESTION_URL=http://cloud-ingestion:3000/api/v1/edge/batch
# Require explicit cloud URL in non-dev environments.
# CLOUD_INGESTION_URL_REQUIRED=true
# Optional: dev-only mode to skip cloud sends and ack locally.
# SYNC_FORWARDER_MODE=noop
# Optional: add auth/headers for cloud target
# CLOUD_AUTH_MODE=api_key  # none|api_key|hmac
# CLOUD_API_KEY=replace-me
# CLOUD_HMAC_SECRET=replace-me
# CLOUD_INGESTION_AUTHORIZATION=Bearer <token>
# CLOUD_INGESTION_HEADERS_JSON={"x-edge-token":"...","x-org-id":"..."}
DD_SERVICE=edge-sync-forwarder
DD_ENV=development
SYNC_INTERVAL_MS=60000
OUTBOX_BATCH_SIZE=100
OUTBOX_LEASE_SECONDS=120
OUTBOX_MAX_ATTEMPTS=10
OUTBOX_BACKOFF_CAP_SECONDS=600
