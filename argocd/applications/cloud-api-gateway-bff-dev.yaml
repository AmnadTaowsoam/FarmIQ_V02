apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cloud-api-gateway-bff-dev
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/farmiq/farmiq-manifests
    targetRevision: dev
    path: helm/cloud-api-gateway-bff
    helm:
      valueFiles:
        - values-dev.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: farmiq-cloud-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  revisionHistoryLimit: 10
