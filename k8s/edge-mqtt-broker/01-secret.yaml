apiVersion: v1
kind: Secret
metadata:
  name: edge-mqtt-broker-secret
  namespace: edge
  labels:
    app: edge-mqtt-broker
type: Opaque
stringData:
  # Password file format: username:hashed_password
  # Generate with: mosquitto_passwd -c passwordfile username
  # Example (admin:password123):
  # passwordfile: |
  #   admin:$7$101$...
  #
  # For production, replace with actual hashed passwords
  # This is a placeholder - update before deploying to production
  passwordfile: |
    # Placeholder - replace with actual password file content
    # Use mosquitto_passwd to generate hashed passwords

